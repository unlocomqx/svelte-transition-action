{
  "name": "svelte-transition-action",
  "version": "0.0.1",
  "type": "module",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "preversion": "npm run build",
    "version": "auto-changelog -p --template keepachangelog && git add CHANGELOG.md",
    "prepublishOnly": "git push && git push --tags && gh-release",
    "format": "prettier --write src/**/*.ts",
    "check": "tsc --noEmit"
  },
  "keywords": [
    "svelte"
  ],
  "files": [
    "src",
    "dist"
  ],
  "author": "unlocomqx",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/unlocomqx/svelte-transition-actions.git"
  },
  "bugs": {
    "url": "https://github.com/unlocomqx/svelte-transition-actions/issues"
  },
  "homepage": "https://github.com/unlocomqx/svelte-transition-actions#readme",
  "description": "",
  "devDependencies": {
    "auto-changelog": "^2.2.1",
    "gh-release": "^7.0.2",
    "svelte": "^3.29.4",
    "typescript": "^4.0.5"
  },
  "peerDependencies": {
    "svelte": "^3.0.0"
  },
  "exports": {
    "import": "./dist/index.js"
  }
}
